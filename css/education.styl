.education
  section
    margin-bottom: 40px
  
  .row h1, .row div
    @extend .col-md-10
    @extend .col-sm-10
    @extend .col-xs-12
    @extend .col-md-offset-1
    @extend .col-sm-offset-1
    padding: 0 10px
  
  h1
    titled()
  
  section
    .row
      margin: 0 0 20px 0
      
      div
        font-size: 14px
        text-align: center
        @media $media-min-desktop
          text-align: left
